```bash
hosts_file=./hosts.txt                #path to file with hosts
path_to_generate_pass=.               #path to directory with password that will be created during execution, ex: /tmp /home/user .
user=someuser                         #for automatic entry if all hosts have the same access data - leave blank if login details are different on each host
password=somepassword                 #for automatic entry if all hosts have the same access data - leave blank if login details are different on each host
```